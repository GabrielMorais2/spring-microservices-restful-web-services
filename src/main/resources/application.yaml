spring:
  h2:
    console:
      path: /h2
      enabled: true
  datasource:
    url: jdbc:h2:file:~/test
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
      hibernate:
        ddl-auto: create
management:
  endpoints:
    web:
      exposure:
        include: "*"
